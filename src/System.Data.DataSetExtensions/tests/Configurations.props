<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <BuildConfigurations>
      netstandard;
      <!-- NETFX configuration added to workaround: https://github.com/dotnet/corefx/issues/26575 needs to be deleted when fixed -->
      netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>