<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netstandard;
      net461-Windows_NT;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      netfx-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>