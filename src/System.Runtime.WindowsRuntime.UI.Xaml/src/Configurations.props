<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netstandard1.1;
      netstandard;
      netcoreapp-Windows_NT;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      uap-Windows_NT;
      uapaot-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>