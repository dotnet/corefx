<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      netcoreapp-Windows_NT;
      netcoreapp-Unix;
      uap-Windows_NT;
      uapaot-Windows_NT;
      $(PackageConfigurations);
    </BuildConfigurations>
  </PropertyGroup>
</Project>
