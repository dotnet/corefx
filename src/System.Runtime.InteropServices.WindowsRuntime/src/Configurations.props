<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      uapaot-Windows_NT;
      netcoreapp-Windows_NT;
      netcoreapp;
      uap-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>