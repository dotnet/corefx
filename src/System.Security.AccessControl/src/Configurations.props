<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      net461-Windows_NT;
      netcoreapp2.0-Windows_NT;
      netstandard;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      netcoreapp-Windows_NT;
      uap-Windows_NT;
      netfx-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>