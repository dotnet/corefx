<Project DefaultTargets="Build">
  <PropertyGroup>
    <BuildConfigurations>
      netstandard;
      netcoreapp-Unix;
      netcoreapp-Windows_NT;
      uap-Windows_NT;
    </BuildConfigurations>
  </PropertyGroup>
</Project>
