<Project>
  <PropertyGroup>
    <PackageConfigurations>
      netstandard;
      uap10.0.16299;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      netcoreapp;
      uap;
      <!-- Use inbox facade on netfx -->
      _netfx;
    </BuildConfigurations>
  </PropertyGroup>
</Project>