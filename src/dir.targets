<Project ToolsVersion="12.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <Import Project="$(BuildDir)src\props\dirtargets.props" />

  <Import Project="$(BuildDir)dir.targets" />

  <Import Project="$(BuildDir)src\targets\getassemblyversion.targets" />

  <Import Project="$(BuildDir)src\targets\getdocumentationfile.targets" />

  <Import Project="$(BuildDir)src\targets\dumptargets.targets" />
</Project>
